import { useBodyContext } from "../../contexts/game.body.context"
import { useGameContext } from "../../contexts/game.context"


export function StartModal (){

    return(
        <div className="body body-modal">
                
        </div>
    )
}